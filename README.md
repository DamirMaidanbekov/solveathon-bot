# 🤖 Telegram Broadcast Bot

Telegram бот для рассылки сообщений всем пользователям по расписанию. Бот автоматически запоминает всех пользователей, которые когда-либо запустили бота, и отправляет им сообщения в определенное время.

## ✨ Функции

- 📅 **Расписание рассылок** - Определяйте время и текст сообщений в `config.py`
- 👥 **Автоматическое сохранение пользователей** - Все, кто запустил `/start`, будут получать сообщения
- 🔔 **Точное время отправки** - Сообщения отправляются точно в указанное время
- 💾 **Сохранение данных** - Информация о пользователях хранится в JSON
- 🛑 **Защита от дубликатов** - Каждое сообщение отправляется только один раз в нужное время

## 📋 Требования

- Python 3.8+
- pyTelegramBotAPI

## 🚀 Установка и запуск

### 1. Клонировать репозиторий
```bash
git clone <repository-url>
cd solveathon-bot
```

### 2. Установить зависимости
```bash
pip install -r requirements.txt
```

### 3. Настроить бота
- Получите BOT_TOKEN от [@BotFather](https://t.me/botfather) в Telegram
- Скопируйте `config_example.py` в `config.py`:
  ```bash
  cp config_example.py config.py
  ```
- Отредактируйте `config.py` и добавьте ваш BOT_TOKEN

### 4. Запустить бота
```bash
python bot.py
```

## 📱 Команды

| Команда | Описание |
|---------|---------|
| `/start` | Начать работу с ботом |
| `/alarm HH:MM [описание]` | Установить новый будильник |
| `/list` | Показать все ваши будильники |
| `/remove HH:MM` | Удалить будильник на указанное время |
| `/send_all сообщение` | Отправить сообщение всем пользователям (админ) |
| `/help` | Показать справку |

## 📝 Примеры использования

```
/alarm 07:30
/alarm 08:00 Проверить почту
/alarm 12:00 Обеденный перерыв
/list
/remove 07:30
```

## 📂 Структура проекта

```
solveathon-bot/
├── bot.py                 # Основной файл бота
├── config_example.py      # Пример конфигурации
├── config.py             # Ваша конфигурация (не коммитится)
├── requirements.txt      # Зависимости Python
├── users.json           # База данных пользователей (автогенерируется)
├── alarms.json          # База данных будильников (автогенерируется)
├── .gitignore           # Файлы для исключения из git
└── README.md            # Этот файл
```

## 🔒 Безопасность

- **BOT_TOKEN** не коммитится (находится в .gitignore)
- Используйте `config_example.py` как шаблон
- Никогда не делитесь вашим BOT_TOKEN

## ⚙️ Конфигурация

Отредактируйте `config.py` для изменения параметров:

```python
BOT_TOKEN = "YOUR_BOT_TOKEN_HERE"  # Ваш токен бота
USERS_DB_FILE = "users.json"       # Файл базы пользователей
ALARMS_DB_FILE = "alarms.json"     # Файл базы будильников
ALARM_CHECK_INTERVAL = 60           # Интервал проверки (секунды)
```

## 🛠️ Разработка

### Структура кода

- **Database класс** - Управление пользователями и будильниками
- **AlarmChecker класс** - Фоновая проверка и отправка будильников
- **Bot handlers** - Обработчики команд бота

### Добавление новых команд

Добавьте новый handler:

```python
@bot.message_handler(commands=['your_command'])
def handle_command(message):
    bot.reply_to(message, "Response")
```

## 📊 База данных

### users.json
```json
{
  "123456789": {
    "user_id": 123456789,
    "username": "username",
    "registered_at": "2025-12-03T10:30:00",
    "active": true
  }
}
```

### alarms.json
```json
{
  "123456789": [
    {
      "time": "07:30",
      "description": "Проснуться",
      "created_at": "2025-12-03T10:30:00",
      "active": true
    }
  ]
}
```

## 🐛 Известные ограничения

- Время проверяется каждые N секунд (по умолчанию 60)
- Будильник сработает один раз в день на указанное время
- Нет поддержки часовых поясов (используется локальное время сервера)

## 📝 Лицензия

Этот проект свободен для использования и модификации.

## 👨‍💻 Автор

Создан с ❤️

---

**Наслаждайтесь вашим ботом! 🎉**
